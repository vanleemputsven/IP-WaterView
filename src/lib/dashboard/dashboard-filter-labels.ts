/** Default device scope label — aligned across URL filters, charts, and history. */
export const ALL_DEVICES_SCOPE_LABEL = "All devices";

/** Default metric scope on history table filters (“any metric”). */
export const ALL_METRICS_SCOPE_LABEL = "All metrics";
