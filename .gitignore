# Dependencies
node_modules
.pnp
.pnp.js

# Build
.next
out
build
dist

# Env (contains secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea
.vscode
*.swp
*.swo

# Prisma — commit the whole prisma/ tree (schema, migrations/*.sql, seed.ts,
# migrations/migration_lock.toml). Needed for reproducible DB changes & CI.
# Optional local SQLite experiments only:
*.db
*.db-journal

docs/*
docs/architecture.md
docs/design-tokens.md
docs/email-templates.md
docs/supabase-setup.md

# Firmware — commit sketches; never commit Wi-Fi / API secrets
firmware/secrets.h
firmware/*

